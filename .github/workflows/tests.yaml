---

name: tests

on: pull_request

jobs:
  scan:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      # Get the version
      - uses: ./
        with:
          version: '1.0.0'


    
